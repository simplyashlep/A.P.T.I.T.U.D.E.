nodeLinker: node-modules

# Allow CI installs even if lockfile needs minor updates (e.g. version migration)
# This fixes the "The lockfile would have been modified by this install, which is explicitly forbidden." error
enableImmutableInstalls: false

# Prefer a specific modern Yarn version if using Corepack
# yarnPath: .yarn/releases/yarn-4.5.0.cjs
