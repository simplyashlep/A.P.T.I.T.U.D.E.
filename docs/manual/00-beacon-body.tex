% This file is reserved for the complete body extracted from the supplied
% Bias Beacon Documentation.tex. It must contain every original section,
% table, agent specification, invariant, CRA inventory, bounds table,
% credibility hierarchy, and build-order entry without summarization.
% The canonical source is the supplied desktop document and is being retained
% unchanged there until the body extraction is completed safely.
